apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "akri-test.fullname" . }}-test-connection"
  labels:
    {{- include "akri-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "akri-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
